import React from 'react';

const Footer = () => {
  const footerStyle = {
    padding: '20px',
    backgroundColor: '#245487',
    color: '#fff',
  };

  return (
    <footer style={footerStyle}>
    </footer>
  );
};

export default Footer;